{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile",
    "config": {
      "dockerfile": "./Dockerfile"
    }
  },
  "deploy": {
    "startCommand": "dotnet server.dll"
  }
}
